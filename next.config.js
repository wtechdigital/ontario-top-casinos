/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['strapi-canada-reviews-u7263.vm.elestio.app'],
      },
}

module.exports = nextConfig
